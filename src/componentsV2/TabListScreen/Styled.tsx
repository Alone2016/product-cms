import { Card } from "@mantine/core";

import styled from "styled-components";

export const ListScreenCardStyled: any = styled(Card)`
  .mantine-Paper-root {
    box-shadow: none;
    border: none;
    overflow: auto !important;
  }
`;
